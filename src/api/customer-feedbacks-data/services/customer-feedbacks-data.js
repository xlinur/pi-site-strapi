'use strict';

/**
 * customer-feedbacks-data service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::customer-feedbacks-data.customer-feedbacks-data');
