'use strict';

/**
 * customer-feedbacks-data router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::customer-feedbacks-data.customer-feedbacks-data');
