'use strict';

/**
 * customer-feedbacks-data controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::customer-feedbacks-data.customer-feedbacks-data');
