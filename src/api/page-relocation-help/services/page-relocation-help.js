'use strict';

/**
 * page-relocation-help service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::page-relocation-help.page-relocation-help');
