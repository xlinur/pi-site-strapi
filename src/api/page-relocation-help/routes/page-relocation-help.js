'use strict';

/**
 * page-relocation-help router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::page-relocation-help.page-relocation-help');
