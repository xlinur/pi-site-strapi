'use strict';

/**
 * page-relocation-help controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::page-relocation-help.page-relocation-help');
