'use strict';

/**
 * section-trusted-map router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::section-trusted-map.section-trusted-map');
