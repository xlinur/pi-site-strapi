'use strict';

/**
 * section-start-conversation-form controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::section-start-conversation-form.section-start-conversation-form');
